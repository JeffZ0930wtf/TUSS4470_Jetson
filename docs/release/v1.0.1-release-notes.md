# V1.0.1 release notes

## Document overview

This document records the V1.0.1 source/software release, its launcher changes,
and the scope of release verification. It supplements the
[V1.0.0 artifact manifest](v1.0.0-manifest.md) and
[known limitations](v1.0.0-known-limitations.md), which continue to define the
accepted acquisition runtime and physical-hardware evidence.

## Release scope

- Software version and annotated Git tag: `1.0.1` / `v1.0.1`.
- Previous release: `v1.0.0`, resolving to `d5ba025ddd0da450c5ea2ab31ca4c9152dbd44d7`.
- Launcher implementation: `3b83ee7eccf007992244ae0e9d9db065a848f1ff`.
- Windows entry: `scripts/start-jetson.ps1`, which invokes the Jetson script
  through SSH, establishes or reuses a local tunnel, and opens the browser.
- Jetson entry: `scripts/start-jetson.sh`, which starts Core/Bridge through
  Compose, checks readiness, and opens a desktop browser when available.
- Both entries require explicit external 7 V confirmation. They do not Apply
  configuration, request acquisition, or intentionally generate a Burst.
- The launchers finish after startup; runtime supervision is not added.
- Windows PowerShell launcher tests are scoped to Windows so they do not
  require Windows PowerShell in the native Jetson test suite.
- The release includes the V1.0.0 documentation corrections and the recorded
  waveform vertical-axis presentation limitation.

## Runtime and artifact compatibility

The initial source release reused the accepted `1.0.0` runtime image. A
deployment follow-up now selects `tuss4470-acquisition-core:1.0.1` by default,
so starting the latest checkout does not select the old image again.

The new native ARM64 image was built from the exact published `v1.0.1` source
at `f52af4455e6247aab7cad50614729c9156ec2897`; its OCI version is `1.0.1` and
its revision label is that full SHA. Its image ID is
`sha256:bedb07481ddcb78362b98127d3fdb50acc863479ad5acb5d34193d72431768a8`.
Version, dependency, protocol, and Core/Bridge entrypoint checks passed in
temporary containers with no hardware devices and no network.

The published `v1.0.1` source tag is unchanged. Follow-up commit
`759262b140f570e55abe3a4645af7cc2924a397a` changes only the default image
selection and its tests/documentation; its 29 project-layout and launcher
checks passed. The previous image
`sha256:b8f0b7326cc76653082a663183a9fcd59c3214d315e9129702600737144ca258`
is retained for rollback.

On 2026-09-14, the Jetson Core and Bridge were actually recreated with the
new ARM64 image. The persistent mounts remained unchanged; the device reported
`NORMAL / IDLE`; capture sequence remained `0` before and after the switch;
and no Apply, capture, or firmware flash was performed. The external evidence
is `D:/Desktop/TUSS4470_data/release/v1.0.1/jetson-image-deployment.json`.

Firmware remains `0.2.0.2`, wire protocol remains v1, and the runtime source,
SQLite schema, raw samples, and configuration/acquisition behavior are unchanged.
Third-party dependency resolutions and `requirements-container.lock.txt` are
unchanged; only the local project's version changes in `uv.lock`.

The accepted canonical firmware HEX SHA-256 is
`e6e2a3716bbf1ab3142cb205ecdac03eb54f2af9625db09b469989da0d833d3f`.

## Verification scope

The complete Windows offline gate ran on
`753710bd1038d1a2744253d835a016223c2c0652` using Python 3.12.14 and pytest 8.4.2.
The tagged release adds only release documentation after that verified source
commit. The later `759262b` host-selection change was separately covered by
the 29 targeted checks described above and does not change runtime code.

| Check | Observed result |
|---|---|
| `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/test-all.ps1` | Exit 0 |
| Python, including five launcher tests | 237 passed in 15.35 s |
| Node Web lifecycle regression | PASS |
| Production firmware and official TI USB stack build | PASS |
| MSP430 simulator and retained safety/static checks | PASS |
| Firmware canonical HEX vs. V1.0.0 baseline | Byte-identical |
| Firmware text/data/bss | 26886 / 98 / 6118 bytes |
| Installed package and four public `--help` commands | Version 1.0.1; all exit 0 |
| Third-party lock resolutions and container lock | Unchanged |
| Windows-only test collection with Linux platform simulated | Two Windows tests correctly skipped |
| Changed Markdown links and whitespace checks | PASS |

Pytest emitted one non-blocking cache warning: the pre-existing Windows ACL
on `.pytest_cache` prevented writing its cache. No tests failed.

Raw evidence is preserved outside Git at
`D:/Desktop/TUSS4470_data/release/v1.0.1/windows/753710b/`.
The `windows-gate.log` SHA-256 is
`9662f60085781a2aeb464ac7e15f3c9cc2be24d00d5db5e4992cf7dbe00f76d0`;
the newly compiled ELF SHA-256 is
`ba9cc3c0aab00f4321fae49d2091e2982107ecb626ba04c2258a70e761f53bc9`.
Firmware equivalence is established by the canonical loadable HEX, not by
requiring the complete ELF container to match an older build directory.

Launcher checks use fake SSH/Docker/device responses and do not establish
a new real-hardware acceptance result.

The initial source-release check did not start physical Core/Bridge services,
access serial devices, flash firmware, apply device configuration, or request
a capture. Native Jetson execution and the new container build were subsequent
deployment follow-ups with the evidence stated above; V1.0.0 remains the
baseline for a physical capture through the full acquisition path.

## Operating limits

Existing Compose restart policies are unchanged. The script's explicit supply
confirmation applies to script invocation; it does not add a new gate to Docker's
automatic container restarts. Readiness is a startup observation, not continuous
power monitoring. Follow the supply/reset procedure in the deployment guide.

Windows tunnel reuse recognizes the existing project's HTTP health/device
response; it does not prove the existing tunnel's destination matches a changed
host argument. Use distinct local ports when switching between Jetson hosts.

The startup timeout is a polling deadline, not a strict deadline around every
external command. An unresponsive Docker/HTTP operation can delay completion.
Browser invocation is best-effort on Jetson; a Windows browser-launch error can
still be reported after the remote services and tunnel have become ready.
