# V1.0.1 Release Plan

> **For agentic workers:** Use `executing-plans` to complete this release inline. The user has authorized integration, push, and the next release tag.

## Document overview

This plan publishes the completed Jetson launchers as V1.0.1. It records the
version decision, release scope, and offline checks, and complements the V1.0.0
artifact manifest and the V1.0.1 release notes.

**Goal:** Publish the current launcher implementation on GitHub main with an annotated `v1.0.1` tag.

**Architecture:** Keep the two launchers from `3b83ee7`: Jetson owns Compose startup; Windows invokes it through SSH and opens the local tunnel/browser. Preserve the accepted acquisition runtime and firmware.

**Tech Stack:** PowerShell, POSIX shell, Python 3.12/uv 0.12.5, Git.

## Global constraints

- GitHub main was verified at `3d1e88342b6692e175a19b2dea976860f75a711a`; the existing annotated `v1.0.0` resolves to `d5ba025ddd0da450c5ea2ab31ca4c9152dbd44d7`.
- Publish software `1.0.1`; preserve the existing `v1.0.0` tag and history.
- Preserve firmware `0.2.0.2`, protocol v1, SQLite schema, and acquisition behavior.
- The launchers continue using the accepted `tuss4470-acquisition-core:1.0.0` image. Record this explicitly; do not relabel an old image as newly built.
- Do not start real services, access serial ports, flash firmware, apply configurations, or acquire data during release verification.
- No new dependencies or unrelated feature work. Keep existing third-party lock resolutions unchanged.

## Task 1: Prepare and verify the release

**Files:** `pyproject.toml`, `uv.lock`, `deploy/Dockerfile.core`, `scripts/test-all.sh`, `tests/integration/test_project_layout.py`, `tests/integration/test_service_launchers.py`, `README.md`, deployment docs, and `docs/release/v1.0.1-release-notes.md`.

- [x] Verify remote main and existing release tags, and identify the clean launcher worktree.
- [x] Check that Linux collects only the applicable launcher tests; fix Windows-only test scoping if needed.
- [x] Update software metadata, future image-build version labels, and corresponding version assertions to 1.0.1; retain the accepted runtime image default at 1.0.0.
- [x] Synchronize the isolated Python environment and run Python/Node checks without hardware access. The suite passed 236 tests; the remaining lock-export check passed after supplying the worktree-local uv binary. Node and all four installed command help checks passed.
- [x] Integrate into main, synchronize its installed package, and run the complete Windows offline gate plus firmware HEX comparison. On `753710bd1038d1a2744253d835a016223c2c0652`: 237 Python tests, Node, production firmware, simulators, and safety/static audits passed; the canonical HEX matched V1.0.0 byte-for-byte.
- [x] Record actual results and limitations in release notes; check whitespace, staged scope, and sensitive literals. Raw evidence is preserved under `D:/Desktop/TUSS4470_data/release/v1.0.1/windows/753710b/`.

## Task 2: Publication procedure and external evidence

Publication follows the final documentation commit. Its completion is recorded
in GitHub's actual refs and the external
`D:/Desktop/TUSS4470_data/release/v1.0.1/publication.json`; it does not require
moving a published tag or adding a post-tag checklist commit.

1. Commit the validated release metadata and evidence. Confirm the final tree
   differs from the tested source only under `docs/`.
2. Create annotated `v1.0.1` and push main and that tag without force. Verify
   GitHub resolves both to the released commit and `v1.0.0` is unchanged.
3. Preserve the observed remote refs in the external publication record and
   report the prior/new version, final SHA, validation, and deployment scope.
